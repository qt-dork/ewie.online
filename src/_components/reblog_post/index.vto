{{> let replying_to = undefined }}

{{ comp reblog_post.wrapper }}

  {{# Reblog content #}}
  {{ if reblogs }}
    {{ for reblog of reblogs }}
      {{ comp.reblog_post.attribution({ author: reblog.author, url: reblog.url, date: reblog.date, replying_to }) }}
      {{> replying_to = { action: "reblog", source: reblog.author.displayName } }}
      <hr />
      {{ if (reblog.title) }}
        {{ comp reblog_post.title { url: reblog.url, is_timeline } }}
          {{ reblog.title }}
        {{ /comp }}
      {{ /if }}
      {{ if reblog.content }}
        {{ comp reblog_post.body }}
          {{ reblog.content |> md }}
        {{ /comp }}
      {{ /if }}
      {{ if reblog.tags }}
        {{ comp.reblog_post.tags({ tags: reblog.tags }) }}
      {{ /if }}
      <hr />
    {{ /for }}
  {{ /if }}

  {{# Main post #}}
  {{ comp.reblog_post.attribution({ author, url, date, replying_to }) }}
  <hr />
  {{ if title }}
    {{ comp reblog_post.title { url, is_timeline } }}
      {{ title }}
    {{ /comp }}
  {{ /if }}
  {{ if content }}
    {{ comp reblog_post.body }}
      {{ content }}
    {{ /comp }}
  {{ /if }}
  {{ if tags }}
    {{ comp.reblog_post.tags({ tags }) }}
  {{ /if }}
  <hr />
  {{ comp.reblog_post.interactibles({ url, is_timeline }) }}
{{ /comp }}
